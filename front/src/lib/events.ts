import { StoreEvent } from "$store";
import type { State } from "$state";

// export class IncreaseCount extends StoreEvent<State> {
//   constructor(private amount: number) {
//     super();
//   }
// 
//   public update(state: State) {
//     state.counter += this.amount;
//   }
// }
